package enum

enum class ConsoleFlag(val nameParam: String) {
    LOGIN("-login"),
    PASSWORD("-pass"),
    RESOURCE("-res"),
    ROLE("-role"),
    DATE_START("-ds"),
    DATE_END("-de"),
    VOLUME("-vol")
}